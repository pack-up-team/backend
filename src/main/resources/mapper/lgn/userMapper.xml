<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swygbro.packup.user.Mapper.UserMapper">
	<select id="selectUserById" parameterType="String" resultType="com.swygbro.packup.config.CustomUserDetails">
		/**
		 * kr.or.seoulphil.cms.common.lgn.mapper.LoginMapper > selectUserById
		 * 작성자 : ahyun
		 * 설명 : 로그인용 사용자 조회
		 * 사용여부는 서비스 로직에 판별하고 다른 메시지를 표출해야 하기 때문에 빼둠
		 */
		 SELECT
			 TU.*
		   FROM TBL_USER TU
		  WHERE USER_ID = #{userId}
		 	AND DEL_YN = 'N'
	</select>
</mapper>