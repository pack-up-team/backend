spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        
  h2:
    console:
      enabled: true
      
  jwt:
    secret: thisIsASecretKeyWithAtLeast32ByteLength123!!

logging:
  level:
    org.springframework.security: DEBUG
    com.swygbro.packup: DEBUG
